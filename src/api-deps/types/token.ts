/**
 * This file defines (or worse, re-exports) types that are shared between the frontend and the backend.
 */

export { TokenId };
enum TokenId {
  ALGO = 'ALGO',
  NEAR = 'NEAR',
  wALGO = 'wALGO',
  goNEAR = 'goNEAR',
}
